pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                script {
                    sh 'export PATH=$PATH:/path/to/python3/bin'
                }
            }
        }
    }
}
